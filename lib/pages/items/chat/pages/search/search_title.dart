import 'package:flutter/material.dart';

class ChatSearchTitle extends StatelessWidget {
  const ChatSearchTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
