import 'package:flutter/material.dart';

class HomeBodyItemGesture extends StatelessWidget {
  const HomeBodyItemGesture({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return GestureDetector();
  }
}
