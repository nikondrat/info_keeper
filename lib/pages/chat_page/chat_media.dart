import 'package:flutter/material.dart';

class ChatPageMedia extends StatelessWidget {
  const ChatPageMedia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
